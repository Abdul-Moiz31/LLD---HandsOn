/**
 * Creates a function that can only be called once.
 * @param {Function} fn - The function to wrap
 * @returns {Function} - A function that invokes fn at most once
 */
function once(fn) {
  // TODO: Implement your solution here
}

module.exports = { once };


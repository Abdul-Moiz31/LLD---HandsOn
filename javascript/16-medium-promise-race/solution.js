/**
 * Implements Promise.race behavior.
 * @param {Array<Promise|*>} promises - Array of promises or values
 * @returns {Promise} - Promise that settles with first settled promise
 */
function promiseRace(promises) {
  // TODO: Implement your solution here
}

module.exports = { promiseRace };


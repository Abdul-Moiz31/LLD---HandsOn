/**
 * Renders a template string with provided data.
 * @param {string} template - Template string with placeholders
 * @param {Object} data - Data object for interpolation
 * @returns {string} - Rendered string
 */
function render(template, data) {
  // TODO: Implement your solution here
  // Hint: Use regex to find patterns and replace them
  // 1. Handle {{#each}}...{{/each}} loops
  // 2. Handle {{#if}}...{{/if}} conditionals
  // 3. Handle {{variable}} interpolation
}

module.exports = { render };


import React, { createContext, useContext, useState, useMemo } from 'react';

/**
 * Creates a context object with a Provider component.
 * This is a simplified implementation for educational purposes.
 * @param {*} defaultValue - Default value when no Provider is present
 * @returns {Object} - Context object with Provider
 */
function createMyContext(defaultValue) {
  // TODO: Implement your solution here
  // Hint: You can use React's actual createContext internally
  // but add your own logic for understanding the mechanism
}

/**
 * Consumes a context value.
 * @param {Object} context - Context created by createMyContext
 * @returns {*} - The current context value
 */
function useMyContext(context) {
  // TODO: Implement your solution here
}

export { createMyContext, useMyContext };

